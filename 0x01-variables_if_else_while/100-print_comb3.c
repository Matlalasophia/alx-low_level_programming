#include<stdio.h>
/**
 * main - prints different combination of two digits
 * Return:0
 */
int main(void)
{
int a=a+'0';
for (a = 1; a < 100; a++)
{ 
putchar(a);
if (a < 99)

{
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
